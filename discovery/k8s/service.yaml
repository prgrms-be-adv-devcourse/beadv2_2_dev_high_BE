apiVersion: v1
kind: Service
metadata:
  name: discovery
  labels:
    app: discovery
spec:
  type: ClusterIP
  selector:
    app: discovery
  ports:
    - name: http
      port: 8761
      targetPort: 8761
