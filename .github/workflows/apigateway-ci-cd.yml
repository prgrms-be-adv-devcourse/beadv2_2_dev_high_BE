name: Apigateway CI/CD

on:
  push:
    branches:
      - feat/#46-ci-cd
    paths:
      - 'apigateway/**'
      - '.github/workflows/apigateway-ci-cd.yml'
      - 'build.gradle'
      - 'settings.gradle'
      - '.dockerignore'
  pull_request:
    paths:
      - 'apigateway/**'
      - '.github/workflows/apigateway-ci-cd.yml'
      - 'build.gradle'
      - 'settings.gradle'
      - '.dockerignore'
  workflow_dispatch:

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository_owner }}/apigateway
  SERVICE_NAME: apigateway
  NAMESPACE: ${{ vars.K8S_NAMESPACE || 'default' }}

jobs:
#  test:
#    runs-on: ubuntu-latest
#    steps:
#      - name: Checkout
#        uses: actions/checkout@v4
#
#      - name: Set up JDK
#        uses: actions/setup-java@v4
#        with:
#          distribution: temurin
#          java-version: 17
#          cache: gradle
#
#      - name: Run apigateway tests
#        run: |
#          chmod +x gradlew
#          ./gradlew :apigateway:test

  build-and-push:
#    needs: test
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
      id-token: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 17
          cache: gradle

      - name: Build apigateway bootJar
        run: |
          chmod +x gradlew
          ./gradlew :apigateway:bootJar

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ secrets.GIT_ACTOR }}
          password: ${{ secrets.GIT_TOKEN }}

      - name: Build and push image
        uses: docker/build-push-action@v6
        with:
          context: .
          file: apigateway/Dockerfile
          push: ${{ github.event_name != 'pull_request' }}
          tags: |
            ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${{ github.sha }}
            ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:latest

  deploy:
    needs: build-and-push
    if: github.ref == 'refs/heads/feat/#46-ci-cd'
    runs-on: ubuntu-latest
    environment: production
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up kubectl
        uses: azure/setup-kubectl@v4

      - name: Configure kubeconfig
        run: |
          mkdir -p ~/.kube
          echo "${{ secrets.KUBE_CONFIG }}" | base64 -d > ~/.kube/config

      - name: Ensure GHCR imagePullSecret exists
        run: | #secret 존재하면 덮어쓰기, 없으면 새로 생성. 도커 레지스트리(secret)에 ghcr 접근관련 정보제공, --dry-run=client -o yaml은 리소스 안남김
          kubectl create secret docker-registry ghcr-secret \
            --namespace ${{ env.NAMESPACE }} \
            --docker-server=${{ env.REGISTRY }} \
            --docker-username=${{ secrets.GIT_ACTOR }} \
            --docker-password=${{ secrets.GIT_TOKEN }} \
            --dry-run=client -o yaml | kubectl apply -f -

      - name: Create or update apigateway secret
        run: | #jwt 제공
          kubectl create secret generic apigateway-secret \
          --namespace ${{ env.NAMESPACE }} \
          --from-literal=JWT_SECRET="${{ secrets.JWT_SECRET }}" \
          --from-literal=JWT_ACCESS_TOKEN_EXPIRATION="${{ secrets.JWT_ACCESS_TOKEN_EXPIRATION }}" \
          --dry-run=client -o yaml | kubectl apply -f -
    

      - name: Deploy apigateway
        env:
          IMAGE: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${{ github.sha }}
        run: |
          kubectl apply -f apigateway/k8s --namespace ${{ env.NAMESPACE }}
          kubectl set image deployment/${{ env.SERVICE_NAME }} ${{ env.SERVICE_NAME }}=$IMAGE --namespace ${{ env.NAMESPACE }}
          kubectl rollout status deployment/${{ env.SERVICE_NAME }} --namespace ${{ env.NAMESPACE }}
