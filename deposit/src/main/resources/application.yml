spring:
  application:
    name: deposit-service
  datasource:
    url: jdbc:postgresql://3.39.162.115:30007/team5?currentSchema=deposit&options=-c%20TimeZone=Asia/Seoul
    driver-class-name: org.postgresql.Driver
    username: postgres
    password: 1234
  jackson:
    time-zone: Asia/Seoul
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        jdbc:
          time_zone: Asia/Seoul
        globally_quoted_identifiers: true
  config:
    import: "configserver:http://localhost:8888"
server:
  port: 8083